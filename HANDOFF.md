# pSEO 실적 대시보드 — 인수인계 문서

이 문서 하나로 다른 AI/사람이 이어서 작업할 수 있게 쓴 것. 실제 코드는 `dashboard.html`, `build_data.py` 에 있고
이 문서는 **왜 그렇게 만들었는지, 무엇을 바꾸면 안 되는지**를 담는다.

최종 갱신: 2026-09-22

---

## 1. 무엇을 만드는가

LF몰 pSEO(프로그래매틱 SEO) 페이지의 **유입 실적**과 **콘텐츠 운영 상태**를 한 화면에서 보는 정적 대시보드.
HTML 한 장 + 파이썬 빌드 스크립트. 서버 없음. 기반 레퍼런스는 사내 `ryunj/first-dashboard`(첫구매 실적 대시보드)의 구조와 디자인 토큰.

메뉴는 2개뿐이다.

| 메뉴 | 목적 |
|---|---|
| **실적 대시보드** | 유입 지표를 기간·차원별로 본다 |
| **리스트 관리** | 업로드한 콘텐츠 대장을 건별로 관리한다 (운영중/미반영/유입없음 점검) |

프로젝트는 **2026년 7월 시작**이다. 전년 데이터가 없으므로 전년 동기간 비교는 넣지 않았다.
비교는 **직전 기간**만 쓰고, 화면 최상단에 "최근 추세"를 둬서 최근 흐름부터 보이게 한다.

---

## 2. 데이터 소스 3종

### 2-1. 실적 RAW — PV·UV 크로스탭 (태블로/어도비 내보내기)

- 파일 인코딩이 **UTF-16 LE, 탭 구분**이다. 확장자는 `.csv` 지만 일반 CSV 파서로는 깨진다.
- 구조가 평범한 테이블이 아니라 **크로스탭**이다.

```
(행 0)  ''  ''      유입_연월(YYYYMM) / 유입_연월일(YYYYMMDD)  ... 반복
(행 1)  ''  ''      202607  202607  ...  202608
(행 2)  ''  AF코드   20260710  20260713  ...  20260829      ← 날짜 헤더
(행 3)  PV  총계     37  90  28  ...                          ← 중복제거 총계
(행 4)  UV  총계     5   7   4   ...
(행 5)  PV  PSBRD1   ''  ''  ''  2  ...                        ← 코드별
(행 6)  UV  PSBRD1   ...
```

- 0열 = 지표(`PV`/`UV`), 1열 = AF코드(또는 `총계`), 2열부터 = 날짜별 값
- 빈 칸은 0. 값은 `37.0` 같은 실수 문자열로 온다
- **파싱 규칙**: 앞 8행 중 `^\d{8}$` 패턴이 가장 많은 행을 날짜 헤더로 잡는다 (열 위치를 하드코딩하지 않는다)

### 2-2. 콘텐츠 대장 — `pSEO_전체대장_YYYYMMDD.xlsx` (시트명 `전체대장`)

현재 업로드된 콘텐츠의 마스터. **AF코드 ↔ 키워드 매핑의 기준이자 리스트 관리 탭의 원천.**

| 컬럼 | 값 예시 | 비고 |
|---|---|---|
| 차수 | `1차` `2차` `3차` | 제작 배치 |
| 키워드 | `닥스 남성 지갑 추천` | = 페이지 제목. 여기서 브랜드·성별·카테고리를 분해 |
| AF코드 | `PSBRD290` | 실적 CSV와의 **유일한 연결고리** |
| slug | `daks-mens-wallet-rcmd` | 실제 URL 조각 |
| 현재 운영중 | `Y` / `N` | |
| 업데이트 날짜 | `2026-09-11` | 비어 있을 수 있음 |
| 최신 반영 | `Y` / `N` | |
| 미반영 분류 | `생성실패` `검수탈락` `검수-치명` / 빈칸 | |
| 미반영 사유 | `[상품부족-검색없음] LF몰에 해당 브랜드의 …` | 길다. 표에서는 말줄임 + title 툴팁 |

2026-09-11 기준 1,134행 (1차 70 / 2차 722 / 3차 342), 운영중 795, 최신반영 711, 생성실패 343, 검수탈락 78, 검수-치명 2.

### 2-3. 구글시트 `★PSEOAF_코드_자동화_시트`

- `PSEO` 탭 — NBOS에 등록된 AF코드 대장. 2,705건 (`PSBRD` 2,395 / `PSCAT` 310)
  주요 컬럼: `매체코드`, `매체코드명(시트기재)`, `리포트 PSEO유형`(`brand`/`cat`), `소`(`PSEO_브랜드`/`PSEO_성별x카테고리`), `GA UTM 파라미터 utm_source`(`PSEO_PSBRD1`)
- `자동화시트` 탭 — 대행사 요청 목록을 붙여 넣어 AF코드를 채번하고 NBOS 업로드 양식으로 변환하는 작업 시트
- CSV로 읽을 때: `…/gviz/tq?tqx=out:csv&sheet=<탭이름>` (공유 링크가 열려 있으면 그대로 받아진다)

---

## 3. ⚠️ AF코드 매핑 충돌 — 미해결

**같은 AF코드가 소스마다 다른 키워드를 가리킨다.**

| 코드 | 콘텐츠 대장 = 자동화시트 | 구글시트 `PSEO` 탭 |
|---|---|---|
| PSBRD2 | 닥스 가방 | 닥스 남성 가디건 |
| PSBRD290 | 알레그리 가방 | 마에스트로 데님팬츠 |
| PSBRD295 | 알레그리 정장 | 마에스트로 정장 |

- 겹치는 402건 중 **일치 12건**. 그런데 구간을 나눠 보면 원인이 뚜렷하다.
  - `PSBRD1~500` : 400건 중 **10건만 일치** → 어긋남
  - `PSBRD501~` : 2건 중 2건 일치 → 정상
- 자동화시트 안내문에 *"1차 변환 시 AF코드 숫자 1씩 커지도록 변경"* 이라고 적혀 있다.
  1차 500건을 NBOS에 올린 뒤 자동화시트에서 정렬을 바꿔 **다시 채번**했고, `PSEO` 탭만 옛 상태로 남은 것으로 보인다.
- **현재 구현은 콘텐츠 대장을 기준으로 쓴다.** 자동화시트와 일치하고, 실제 페이지에 박히는 코드는 제작 쪽에서 나오기 때문.
- **확인해야 할 것**: 태블로/NBOS 리포트가 코드를 어떤 이름으로 보여주는지. 만약 `PSEO` 탭 이름 기준이면
  1차 500건의 브랜드·카테고리 집계가 전부 틀어진다. 이 경우 `build_data.py` 의 매핑 우선순위만 뒤집으면 된다.
- 실적은 있는데 대장에 없는 코드 4건: `PSBRD502`, `PSBRD539`, `PSBRD728`, `PSBRD1443`
  (이 4건은 두 시트가 서로 일치한다). 화면 상단에 건수로 표시한다.

---

## 4. 키워드 → 브랜드 · 성별 · 카테고리 분해

`닥스 남성 지갑 추천` → 브랜드 `닥스` / 성별 `남성` / 카테고리 `지갑`

1. 끝의 ` 추천` 제거
2. **브랜드 사전을 긴 이름부터** 매칭 (`build_data.py` 의 `BRANDS`)
   `닥스 런던 골프`, `위크엔드 막스마라`, `질 바이 질스튜어트`, `질스튜어트 뉴욕`, `막스마라 선글라스`,
   `아일랜드 슬리퍼`, `오피신 제네랄`, `포르테 포르테`, `티톤 브로스`, `이에르 로르` … 순으로 먼저 보고,
   그 다음 `막스마라`, `질스튜어트`, `닥스` 같은 짧은 이름을 본다. 순서를 지키지 않으면 `질스튜어트 뉴욕 가방`이
   브랜드 `질스튜어트` + 카테고리 `뉴욕 가방`으로 잘린다
3. 남은 앞부분이 `남성`/`여성`/`공용`/`아동`이면 성별로 뗀다. `여성지갑`처럼 **붙여 쓴 경우도 처리**
4. 나머지가 카테고리. 비면 `기타`
5. 브랜드가 안 잡히면 유형 `cat`(성별×카테고리), 잡히면 `br`(브랜드). `PSCAT****` 코드는 무조건 `cat`
6. 브랜드형인데 브랜드가 비면 `(미매핑)` 으로 채운다 — 안 그러면 필터에서 소리 없이 사라진다 (실제로 겪은 버그)

현재 브랜드 32종, 카테고리 138종.

---

## 5. 지표 · 기간 규칙 (바꾸지 말 것)

### 지표

| 지표 | 상태 |
|---|---|
| 유입(PV) · 유입(UV) | **연결 완료** |
| 고객수 · 거래액 · 첫구매고객수 · 첫구매거래액 | 타일·표에 자리만 잡아둠 (`MET` 의 `on:false`) |

구매 지표 RAW가 `일자 × 매체코드` 형태로 오면 `MET` 의 `on` 을 켜고 빌드에 컬럼만 추가하면 된다.
**흐름 차트에는 연결된 지표만 그린다** (`LIVE` = `MET.filter(m=>m.on)`).

### UV 중복제거 — 반드시 이해할 것

UV는 "몇 명이 왔나"이므로 **중복제거 범위**가 값을 바꾼다.

- 한 사람이 하루에 `닥스 가방`과 `헤지스 셔츠` 두 페이지를 보면 → 코드별 UV 각 1, **합치면 2**
- 태블로 `총계` 행은 그날 pSEO에 온 사람을 **1명**으로 센다
- 실측: PV는 코드 합산 = 총계 1,469로 **정확히 일치**, UV는 합산 897 vs 총계 874 → **23 차이**

구현:
- 필터가 없을 때 → UV 타일에 **코드 합산값과 중복제거 총계를 함께** 표시
- 필터가 걸리면 → 그 조합의 중복제거 값을 태블로가 주지 않으므로 **코드 합산만** 쓰고 화면에 그 사실을 적는다
- 브랜드별·카테고리별 UV를 더해도 전체 UV가 안 되는 것은 **정상**이다

### 기간

- 월별 / 주별 / 일별 × **일평균 / 누계**
- 주차는 **ISO 월요일~일요일**
- 비교는 **직전 기간**만. `prevId()` 는 정렬된 기간 배열의 바로 앞 원소를 준다
  (전년 비교는 2027년 7월 이후에 `prevId()` 에 다시 넣는다)
- **일평균의 분모 = 적재된 일자 수.** 필터 때문에 유입이 0인 날이 생겨도 분모를 줄이지 않는다.
  분모는 `D.total`(총계 행이 있는 날짜)에서 만든다
- 유입이 0인 기간도 축에서 빼지 않는다 (추세가 끊겨 보이면 안 되므로)
- "최근 N영업일"은 달력 N일이 아니라 **유입이 적재된 날 N개**다. 적재가 띄엄띄엄하기 때문
- 파생 비율은 기간 합계를 먼저 낸 뒤 다시 계산한다. 행별 비율을 평균내지 않는다

---

## 6. 화면 구성

### 실적 대시보드

1. **최근 추세** — 최근 7영업일 / 14영업일 PV·UV, 직전 동일 구간 대비 증감, 일평균
2. **실적 요약** — 지표 6개 타일 (미연결 4개는 `–` + "RAW 미연결")
3. **흐름** — PV·UV 라인 차트. 실선 = 당기, 점선 = 직전 기간
4. **기간별 실적** — 지표마다 `당기 / 직전 / 증감률` 3단. 기준 기간 열 하이라이트. `당기만` 모드 있음
5. **차원별 실적** — 브랜드 / 카테고리 / 성별 / 페이지 전환, 정렬·상위 N·구성비 막대

상단 필터: 페이지 유형(브랜드 · 성별×카테고리 · CEP) / 성별 칩 / 브랜드·카테고리 멀티선택(검색 가능).
CEP는 아직 AF코드가 없어 비활성. 코드가 등록되면 자동으로 켜진다.

### 리스트 관리

- 타일 6개: 등록 페이지 / 현재 운영중 / 최신 반영 / 생성실패 / 유입 발생 / 누적 PV·UV
  → **"운영중인데 유입 0"** 건수를 같이 띄운다. 색인·노출 점검 대상이라 이게 핵심 숫자다
- 분포 차트 3개: 차수별 페이지 수 · 미반영 분류 · 브랜드별 UV 상위
- 페이지 리스트: 컬럼 머리글 클릭 = 정렬, 바로 아래 칸 = **컬럼별 필터**
  (선택형은 드롭다운, 키워드/AF코드/slug/사유는 포함 검색). 빠른 보기 버튼 `전체` `운영중` `미반영` `운영중·유입없음`

---

## 7. 파일 구조와 갱신 흐름

```
LF_pSEO/
├─ dashboard.html      대시보드 본체 (data.js 를 읽음)
├─ index.html          dashboard.html 로 이동
├─ build_data.py       실적 CSV + 대장 xlsx → data.js
├─ app.py              Streamlit 래퍼 (Main file path)
├─ requirements.txt
├─ README.md           쓰는 법
├─ AGENTS.md           작업 규칙
├─ HANDOFF.md          이 문서
└─ .gitignore          data.js · xlsx · csv 제외
```

**저장소가 public 이므로 실적 수치·대장·`data.js` 는 커밋하지 않는다.**

갱신 방법 2가지:

1. **브라우저** — `dashboard.html` 을 열고 파일을 끌어다 놓거나 `데이터 갱신` 버튼. 파일 종류는 헤더로 자동 판별
   - 헤더에 `YYYYMMDD` 날짜 열 → 실적 CSV (UTF-16 자동 감지)
   - 헤더에 `AF코드` + `키워드` → 콘텐츠 대장 CSV
   - 헤더에 `매체코드` → 구글시트 `PSEO` 탭 CSV (보조 매핑)
   - xlsx 는 브라우저가 못 읽는다 → CSV 로 저장해서 올린다
2. **로컬 빌드** — `python3 build_data.py pSEO_UV.csv pSEO_전체대장_YYYYMMDD.xlsx` → `data.js`

`data.js` 구조:

```js
window.PSEO_DATA = {
  asof: "2026-08-29", from: "2026-07-10", built: "...",
  source: { perf: "...", map: "..." },
  metrics: ["pv","uv"],
  codes: [ { c:"PSBRD1", n:"닥스 가디건", t:"br", b:"닥스", g:"", k:"가디건",
             src:"ledger", ch:"2차", slug:"daks-cardigan-rcmd", live:"Y",
             upd:"2026-09-03", new:"Y", miss:"", why:"" }, ... ],
  rows:  [ ["2026-07-10", 82, 3, 1], ... ],   // [날짜, codes 인덱스, pv, uv]
  total: { "2026-07-10": [37, 5], ... }        // 태블로 중복제거 총계 [pv, uv]
}
```

---

## 8. 현재 수치 스냅샷 (2026-09-22 기준)

- 유입 기간 2026-07-10 ~ 2026-08-29, 적재 37일
- 누적 PV 1,469 / UV 897 (중복제거 874)
- 등록 코드 1,138건 = 대장 1,134 + 대장 밖 4
- 유입 발생 406건 / 운영중 795건 → **운영중인데 유입 0 = 394건**
- 실적이 찍힌 코드는 전부 `PSBRD`(브랜드형). `PSCAT`(성별×카테고리) 310건은 9/22 등록이라 아직 유입 없음
- CEP 유형 AF코드는 아직 미등록

---

## 9. 남은 일

1. **AF코드 매핑 충돌 확정** (3장) — 태블로 리포트 기준 확인 후 우선순위 확정
2. 고객수 · 거래액 · 첫구매고객수 · 첫구매거래액 RAW 연결
3. CEP 유형 AF코드 등록 후 필터 노출 확인
4. 전년 동기간 비교 (2027년 7월 이후)
5. 유입 없는 운영중 페이지 394건 점검 — 색인 여부 / slug 연결 / AF코드 삽입 누락 구분

---

## 10. 다른 AI에게 이어서 시킬 때

이 문서와 함께 아래를 붙여 주면 된다.

- `dashboard.html` (구현 기준)
- `build_data.py` (빌드 규칙)
- 실적 CSV 샘플, 콘텐츠 대장 xlsx

요청 예시:

> 첨부한 HANDOFF.md 는 LF몰 pSEO 실적 대시보드의 인수인계 문서다. dashboard.html 이 현재 구현이고
> build_data.py 가 빌드 스크립트다. 5장의 지표·기간 규칙과 3장의 AF코드 매핑 충돌 내용을 그대로 지킨 채
> <바꾸고 싶은 것> 을 반영해 줘. 기존 지표 정의와 기능은 지우지 말고, 바꾼 부분과 이유를 같이 알려 줘.
