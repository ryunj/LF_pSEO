# pSEO 실적 대시보드

LF몰 pSEO(프로그래매틱 SEO) 페이지의 유입과 콘텐츠 운영 상태를 보는 정적 대시보드다.
Streamlit 은 정적 HTML 을 띄우는 얇은 래퍼이며, **실적·대장 데이터는 저장소에 커밋하지 않는다.**

## 구조

- `dashboard.html` : 화면, 상태, 지표 계산, 기간 비교, 필터, 브라우저 CSV 파서의 기준 구현
- `build_data.py`  : 실적 CSV + 콘텐츠 대장(xlsx) → `data.js`
- `app.py`         : Streamlit 배포 (Main file path `app.py`)
- `index.html`     : `dashboard.html` 로 이동

## 지표·기간 규칙 (바꾸지 않는다)

- 공식 지표는 `dashboard.html` 의 `MET` · `agg()` · `val()` · `prevId()` 다.
- 주차는 ISO 월요일~일요일. 비교는 **직전 기간**만 쓴다 — 2026년 7월 시작 프로젝트라 전년 데이터가 없다.
  전년 비교는 데이터가 1년 쌓인 뒤 `prevId()` 에 다시 넣는다.
- 일평균의 분모는 **적재된 일자 수**다. 필터 때문에 유입이 0인 날이 생겨도 분모를 줄이지 않는다.
- UV 는 코드 합산값이다. 코드 간 중복이 포함되므로 필터가 없을 때는 태블로 중복제거 총계를 함께 보여 준다.
- 파생 비율은 기간 합계를 먼저 낸 뒤 다시 계산한다. 행별 비율을 평균내지 않는다.

## AF코드 매핑 (중요)

- **콘텐츠 대장(`전체대장` 시트)의 AF코드 ↔ 키워드가 기준**이다.
- 구글시트 `★PSEOAF_코드_자동화_시트` 의 `PSEO` 탭은 `PSBRD1~500` 구간에서 대장과 다른 이름을 갖고 있다
  (자동화시트에서 코드를 다시 채번한 뒤 PSEO 탭이 갱신되지 않은 것으로 보임). 501번 이후는 서로 일치한다.
- 대장에 없는 코드는 `src` 가 `ledger` 가 아니며 화면 상단에 건수를 표시한다.
- 브랜드·성별·카테고리는 키워드에서 분해한다. 브랜드 사전은 `build_data.py` 의 `BRANDS` 이며 긴 이름부터 매칭한다.

## 작업 후 확인

- `python -m compileall -q app.py build_data.py`
- 브라우저에서 실적 CSV + 대장 CSV 를 올려 빈 화면 → 렌더까지 확인
- 지표·기간 로직을 건드렸으면 대표 주차의 기존 값과 새 값을 대조
- 실적 수치, 대장, `data.js` 는 커밋하지 않는다 (`.gitignore` 확인)
